<div class="card">

	{% for category in post.terms('section') %}
		<a href="{{ category.link }}">{{ category.name }}</a>
	{% endfor %}

	<h3><a href="{{ post.link }}">{{ post.title }}</a></h3>
	
	{% if post.thumbnail %}
		<img src="{{ post.thumbnail.src }}" alt="" />
	{% endif %}

</div>
