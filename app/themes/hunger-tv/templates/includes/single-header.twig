<header>
	<div class="row">
		<div class="col lg:6">
			<div class="uppercase">
				{% for item in post.terms('section') %}
					<a href="{{ item.link }}">{{ item.name }}</a>
				{% endfor %}
			</div>
			<div>{{ post.date|date }}</div>
		</div>
		<div class="col lg:6">
			{% include 'includes/share.twig' %}
		</div>
	</div>
	<div class="row">
		<div class="col lg:9 lg:offset-2">
			<h1 class="omega">{{ post.title }}</h1>
			<p class="beta"><code>post.standfirst</code><br>{{ post.standfirst }}</p>
			<p class="beta"><code>post.article_excerpt</code><br>{{ post.article_excerpt }}</p>
		</div>
	</div>
</header>
