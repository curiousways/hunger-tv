{% if not image %}
	{% set image = item.image %}
{% endif %}

{% if image %}
	<img
		src="{{ Image(image).src }}"
		{{ classes ? 'class="' ~ classes ~ '"' }}
		alt="{{ Image(image).alt }}"
		loading="lazy"
	/>
{% endif %}
