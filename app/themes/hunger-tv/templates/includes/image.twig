{% if not image %}
	{% set image = item.image %}
{% endif %}

{% if image %}
	<img
		src="{{ Image(image).src(size) }}"
		{{ classes ? 'class="' ~ classes ~ '"' }}
		alt="{{ Image(image).alt }}"
		loading="lazy"
	/>
{% endif %}
