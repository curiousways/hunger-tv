{% if not image %}
	{% set image = post.hero_image %}
{% endif %}

{% set image = Image(image) %}

{# <img
	src="{{ image.src('hero') }}"
	srcset="{{ image.src('portrait') }} 400w, {{ image.src('hero--small') }} 960w, {{ image.src('hero') }} 1920w"
	sizes="100vw"
	class="hero__image"
	alt="{{ hero.alt }}"
	loading="lazy"
/> #}

<picture class="hero__image">
	<source
		media="(max-width: 599px)"
		srcset="{{ image.src('portrait--large') }}"
	>
	<source
		media="(min-width: 600px)"
		srcset="{{ image.src('hero') }}"
	>
	<img
		src="{{ image.src('hero') }}"
		alt="{{ image.alt }}"
		{# loading="lazy" https://make.wordpress.org/core/2021/07/15/refining-wordpress-cores-lazy-loading-implementation/ #}
	/>
</picture>
