{% extends "base.twig" %}

{% block content %}

	{% for post in posts %}
		{% include ['includes/card--' ~ post.post_type ~ '.twig', 'includes/card.twig'] %}
	{% endfor %}
	
	{% include 'includes/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}

{% endblock %}
