<section>
	<div class="row">
		<div class="col lg:8 lg:offset-2">
			<div class="rte">
				{% if item.title %}
					<h2>{{ item.title }}</h2>
				{% endif %}
				{{ item.body }}
			</div>
		</div>
	</div>
</section>
