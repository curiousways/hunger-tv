{% extends "base.twig" %}

{% block content %}

	<h1>{{ post.title }}</h1>

	{% for item in post.meta('article_builder') %}
		{% include 'editorial-legacy/' ~ item.acf_fc_layout|replace({'_': '-'}) ~ '.twig' %}
	{% endfor %}

{% endblock %}
