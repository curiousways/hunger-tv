<p><mark>gallery.twig</mark></p>

<section>
	<div class="row">
		<div class="col">
			<h2>{{ item.name }}</h2>
			{% for image in Image(item.gallery) %}
				{% include 'includes/image.twig' %}
			{% endfor %}
		</div>
	</div>
</section>
