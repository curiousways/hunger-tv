<p><mark>video.twig</mark></p>

{% if item.embed_url or item.poster %}
	<section>
		<div class="row">
			<div class="col">
				{% if item.type == 'embed' %}
					<div class="embed">
						{{ item.embed_url }}
					</div>
				{% else %}
					{# HTV Player (legacy, no idea what it's meant to do) #}
					{% include 'includes/image.twig' with { image: item.poster } %}
				{% endif %}
			</div>
		</div>
	</section>
{% endif %}
