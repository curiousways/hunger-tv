{% extends "base.twig" %}

{% block content %}

	<article>

		<header>
			<div class="row">
				<div class="col">
					<h1>{{ post.title }}</h1>
				</div>
			</div>
		</header>

		{% if post.content %}
			<section>
				<div class="row">
					<div class="col lg:8 lg:offset-2">
						<div class="rte">
							{{ post.content }}
						</div>
					</div>
				</div>
			</section>
		{% endif %}

	</article>

{% endblock %}
