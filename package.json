{
  "name": "spark",
  "version": "1.0.0",
  "description": "Spark is a WordPress boilerplate for Timber incorporating Flynt's build process.",
  "author": "Matthew Dixon <matthewdixondev@gmail.com>",
  "license": "MIT",
  "scripts": {
    "setup:dev": "composer install && cd app/themes/hunger-tv && composer install && yarn install",
    "setup:production": "composer install && cd app/themes/hunger-tv && composer install",
    "watch": "cd app/themes/hunger-tv && yarn watch"
  }
}
